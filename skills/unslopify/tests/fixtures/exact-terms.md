# Workflow vocabulary

Design tickets as independently verifiable vertical slices suitable for one worktree, each stating behavior, blockers, affected seams, acceptance criteria, verification requirements, and parallel safety.

Publish issues using repository-defined labels and native dependency edges via blocked_by with database IDs read before each edge is created; the human-readable Blocked by list stays fallback.
