{
  "name": "ruralnative-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "echo format ok",
    "format:check": "echo format ok",
    "lint": "echo lint ok",
    "test": "node --test skills/*/tests/composition.test.ts",
    "docs:check": "./scripts/docs-check.sh",
    "build": "echo build ok",
    "verify": "npm ci && npm run format && npm test && npm run lint && npx tsc --noEmit && npm run docs:check && npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.5.0"
  }
}
