{
  "name": "ruralnative-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test skills/*/tests/*.test.ts tests/*.test.ts",
    "docs:check": "./scripts/docs-check.sh",
    "verify": "npm ci && npm test && npx tsc --noEmit && npm run docs:check"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.5.0"
  }
}
