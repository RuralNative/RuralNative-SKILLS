1. Say the task goal.
2. Read only the matching row and its relevant sources.
3. Follow the owning seam and its `Not here` routes.
4. Apply the durable documentation policy below; code wins.
5. Put work docs in the tracker; decide invariant conflicts first.
<!-- managed: document-for-agents · revision-evidence: none -->
<!-- unslopify:session-start:start -->
Load `unslopify` before the first user-visible response in every session. Keep it active for all model-authored English questions and prose.
<!-- unslopify:session-start:end -->

## Before starting work

- Use `/unslopify` for all questions, discussions, and other prose addressed to the user.
- Use `/ponytail` before creating or modifying code.
- Use the docs generated by `/document-for-agents` to understand why the code is designed as it is and where changes belong. Follow the orientation route below and read only relevant sources.
- Update durable documentation in the same change only when the code changes a documented decision, invariant, vocabulary term, ownership boundary, policy, or external contract. Otherwise leave docs alone. Code wins when they disagree.

These five commands are the attention contract of the document-for-agents
lifecycle (ADR-0017, ADR-0032): state the goal before reading, read only
relevant sources, and stop for a decision when work
collides with a numbered invariant.

Worktree execution (Kilo Code VS Code extension): when running on the Kilo Code VS Code IDE extension, generate all worktrees through Agent Manager. Record stays here for audit; no rerun is required for the current change.

## Documentation (doc-cache)

This repo runs the document-for-agents lifecycle on its own docs.

- Orientation: `AGENTS.md` → `ARCHITECTURE.md` → the seam's leaf doc →
  `CONTEXT.md`.
- The docs tree is a cache. Apply the durable documentation policy above
  when changing code. On a doc/code conflict, code wins; fix the doc and
  flag the discrepancy.
- Work docs (plans, audits) live in the issue tracker, never the repo.
- Run `./scripts/docs-check.sh` before finishing; a red harness is a work
  item, not a warning.

## Agent skills

### Code review

This repository has no `REVIEW.md`. Reviews use `/review-this` defaults and the applicable repository instructions and configuration. Do not create a review policy file unless explicitly requested.

### Issue tracker

Issues and PRDs live as GitHub issues in this repo, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary — `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
